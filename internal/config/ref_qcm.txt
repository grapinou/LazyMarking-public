#set text(11pt, font: "Liberation Sans")
#set par(justify: true)

// Measure identity independently: a tall header must never move the QR upward.
#let identification = [
  #set text(10pt, weight: "regular")
  #set par(justify: false, leading: 2pt)
  #stack(spacing: 3pt,
    rect(width: 100%, inset: 6pt, radius: 3pt, stroke: 0.6pt)[
      #stack(spacing: 4pt,
        text(11pt, weight: "bold", exam),
        [#text(8pt)[Élève] #text(11pt, weight: "bold", student)],
        [#text(8pt)[Classe] #classCode],
      )
    ],
    if show_marking_instruction [
      #text(8pt)[Répondez au stylo bleu ou noir. Coloriez complètement le/les cercle(s) correspondant(s) à votre/vos réponse(s).]
    ],
  )
]
#show: body => context {
let identification-height = measure(identification, width: 190mm - 225pt).height
let top-margin = calc.max(5cm, identification-height + 12pt)
set page(
  paper: "a4",
  margin:(
  top: top-margin,
  bottom: 1.5cm,
  x: 1cm,
  ),
  header-ascent: top-margin - 3.5cm,
  numbering: "1 / 1",
  header:[
    #let current_page = {context [#here().page()]}
    #text(16pt, weight: "bold")[
    #grid(columns: (105pt, 1fr, 120pt),
          align(left)[#square(size: 95pt)],
          align(left)[#box(width: 100%, height: 95pt)[#place(top + left)[
            #box(width: 100%, height: identification-height)[#identification]
          ]]],
          align(right)[
            #grid(
              columns: (1fr),
              rows: (auto, 70pt),
              text(2em)[#align(right)[\u{25E5}]],
              text(2em)[#align(right)[ \u{25E3} #text(60pt)[#current_page]]]
            )

          ]
          )]
  ],

  footer: [
    #text(30pt)[
    #grid(columns: (1fr, 1fr, 1fr, 1fr, 1fr, 1fr),
    align(left)[\u{25E3}],
    text(1.7em, baseline: -11pt)[#align(center)[\u{25B6}]],
    text(1.3em, baseline: -4pt)[#align(center)[\u{25B2}]],
    text(1.3em, baseline: -3pt)[#align(center)[\u{25BC}]],
    text(1.7em, baseline: -10pt)[#align(center)[\u{25C0}]],
    align(right)[\u{25E2}],
  )
  ]
]
)

body
}
